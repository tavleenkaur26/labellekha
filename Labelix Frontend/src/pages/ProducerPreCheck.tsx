import { Page } from './NewScan'; import type { NavigateFn } from '../types'; export default function ProducerPreCheck({navigate}:{navigate:NavigateFn}){return <Page title="Producer pre-check" subtitle="Run the same backend compliance pipeline before a product reaches inspection."><div className="panel narrow"><h3>Start with a label image</h3><p className="muted">The pre-check uses the existing scan endpoint. Add product metadata if available so the result is easier to trace later.</p><button className="primary-button" onClick={()=>navigate('new-scan')}>Start pre-check</button></div></Page>}
