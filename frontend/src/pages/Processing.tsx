import { useEffect, useState } from 'react'; import type { NavigateFn } from '../types'; import { api } from '../utils/api';
export default function Processing({navigate}:{navigate:NavigateFn}){const [message,setMessage]=useState('Reading the scan result…'); useEffect(()=>{const id=Number(localStorage.getItem('labelix_last_scan')); if(!id){navigate('new-scan');return}let active=true;(async()=>{try{const r=await api.getScan(id);if(!active)return;if(r.status==='recapture_needed'){setMessage('The image could not be evaluated reliably. Please capture a clearer label.');setTimeout(()=>navigate('new-scan'),1400)}else{setMessage('Compliance result ready.');setTimeout(()=>navigate('inspection-result'),500)}}catch(e){setMessage(e instanceof Error?e.message:'Unable to retrieve the scan.')}})();return()=>{active=false}},[navigate]);return <div className="processing-page"><div className="processing-card"><div className="spinner"/><span className="eyebrow">LIVE BACKEND</span><h1>Processing label</h1><p>{message}</p></div></div>}
