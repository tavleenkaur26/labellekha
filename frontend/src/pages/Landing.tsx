import type { NavigateFn } from '../types';

export default function Landing({ navigate }: { navigate: NavigateFn }) {
  return <div className="landing-page"><header className="landing-header"><div className="brand-inline"><span className="brand-mark">LL</span><strong>LabelLekha</strong></div><button className="secondary-button" onClick={()=>navigate('login')}>Sign in</button></header><main className="landing-main"><span className="eyebrow">LEGAL METROLOGY COMPLIANCE</span><h1>Make label checks easier to inspect, explain and report.</h1><p>LabelLekha brings the existing OCR and rule-engine workflow into one practical review interface for product labels.</p><div className="landing-actions"><button className="primary-button" onClick={()=>navigate('login')}>Open workspace</button><span>Built around the connected compliance API.</span></div><div className="landing-grid"><div><strong>01</strong><h3>Capture</h3><p>Upload a clear label image with the product context available at inspection time.</p></div><div><strong>02</strong><h3>Check</h3><p>Read OCR evidence and clause-level results returned by the backend rule engine.</p></div><div><strong>03</strong><h3>Report</h3><p>Review flagged cases and download the generated PDF or CSV record.</p></div></div></main></div>;
}
